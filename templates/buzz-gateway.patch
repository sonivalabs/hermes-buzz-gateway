# Optional adapter improvement: reply-in-own-thread dispatcher
#
# Applies to: <hermes-agent>/plugins/platforms/buzz/adapter.py
# Use ONLY when you must keep the agent in shared channels (can't scope
# BUZZ_CHANNELS) AND want it to continue its OWN threads but not reply to
# everything. Otherwise prefer BUZZ_CHANNELS scoping (simpler).
#
# Requires BUZZ_REQUIRE_MENTION=true.
#
# After applying: sudo systemctl restart hermes-gateway

# 1) In __init__ (near _poll_count): add
#        self._own_message_ids: set = set()

# 2) In send() after _mark_seen(...): add
#        self._own_message_ids.add(str(event_id))

# 3) In seed/poll, track own messages in _handle_event after de-dupe:
#        if str(event.get("pubkey") or "").lower() == self._self_pubkey:
#            self._own_message_ids.add(event_id)

# 4) Replace the mention gate:
#        reply_to_self = self._references_own_message(event)
#        if not is_dm and self.require_mention and not self._is_mentioned(content) and not reply_to_self:
#            return

# 5) Add helper next to _is_mentioned:
#    def _references_own_message(self, event: dict) -> bool:
#        if not self._own_message_ids:
#            return False
#        for tag in event.get("tags") or []:
#            if isinstance(tag, list) and tag and tag[0] == "e" and len(tag) > 1:
#                if str(tag[1]) in self._own_message_ids:
#                    return True
#        return False
